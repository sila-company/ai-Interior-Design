{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter @workspace/api-server --filter @workspace/mockup-sandbox --parallel run dev",
    "preinstall": "node scripts/ensure-pnpm.mjs",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:deploy": "pnpm install --frozen-lockfile && pnpm --filter @workspace/mockup-sandbox run build && pnpm --filter @workspace/api-server run build",
    "catalog:import-apify": "node scripts/import-apify-amazon-products.mjs",
    "catalog:build-seed": "node scripts/build-product-seed-from-csv.mjs",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
